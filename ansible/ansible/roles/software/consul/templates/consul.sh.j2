#!/bin/bash
# {{ ansible_managed }}

bind_addr = "{{ ansible_eth1.ipv4.address }}"
data_dir = "/opt/consul"
client_addr = "0.0.0.0"
ui = true

{% if ansible_hostname == 'SERVER' %}
    server = true
    bootstrap_expect = 1
{% else %}
    retry_join = ["192.168.1.2"]
{% endif %}
